{
  "name": "ether-tweet",
  "license": "UNLICENSED",
  "scripts": {
    "codegen": "graph codegen",
    "build": "graph build",
    "deploy": "graph deploy --node https://api.thegraph.com/deploy/ --ipfs https://api.thegraph.com/ipfs/ kingzee/ether-tweet",
    "create-local": "graph create --node http://localhost:8020/ kingzee/ether-tweet",
    "remove-local": "graph remove --node http://localhost:8020/ kingzee/ether-tweet",
    "deploy-local": "graph deploy --node http://localhost:8020/ --ipfs http://localhost:5001 kingzee/ether-tweet"
  },
  "dependencies": {
    "@graphprotocol/graph-cli": "0.19.0",
    "@graphprotocol/graph-ts": "0.19.0"
  }
}
